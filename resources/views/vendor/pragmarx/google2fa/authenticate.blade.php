@extends('layouts.nexus')

@section('title', 'Two-Factor Authentication | ' . config('app.name'))

@section('content')
<div class="container py-5" style="max-width: 500px;">
    <div class="card glass-card border-0 shadow-lg rounded-4 p-4 p-sm-5 text-center">
        <div class="mb-4">
            <div class="rounded-circle bg-primary-subtle text-primary d-inline-flex p-3 mb-3">
                <i class="fa-solid fa-shield-halved fa-2x"></i>
            </div>
            <h3 class="text-white font-weight-bold mb-1">Two-Factor Authentication</h3>
            <p class="text-white-50 small mb-0">Enter the 6-digit code generated by your authenticator app to continue.</p>
        </div>

        @if($errors->any())
            <div class="alert alert-danger py-2 px-3 small rounded-3 mb-4 text-start">
                <i class="fa-solid fa-circle-exclamation me-2"></i>{{ $errors->first() }}
            </div>
        @endif

        <form action="{{ route('2fa.authenticate') }}" method="POST">
            @csrf
            <div class="mb-4">
                <input type="text" name="one_time_password" class="form-control form-control-lg text-center font-monospace tracking-widest bg-dark text-white border-secondary rounded-3" placeholder="000000" maxlength="6" autofocus required inputmode="numeric">
            </div>

            <button type="submit" class="btn btn-primary font-weight-bold w-100 py-3 rounded-3 fs-6">
                <i class="fa-solid fa-lock-open me-2"></i> Verify & Authenticate
            </button>
        </form>

        <p class="text-white-50 small mt-4 mb-0">
            Having trouble? Contact support or use your account recovery key.
        </p>
    </div>
</div>
@endsection
