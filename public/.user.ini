; Per-directory PHP limits (used by many FPM/CGI setups). Reload PHP-FPM after deploy if changes are ignored.
; post_max_size must be >= upload_max_filesize.
upload_max_filesize = 64M
post_max_size = 64M
max_file_uploads = 50
max_execution_time = 120
max_input_time = 120
